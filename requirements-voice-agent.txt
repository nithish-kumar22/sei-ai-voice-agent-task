# Voice agent worker: install after main requirements
# pip install -r requirements.txt && pip install -r requirements-voice-agent.txt
-r requirements.txt
livekit-agents[openai,deepgram,silero,turn-detector]>=1.4.0
livekit-plugins-piper-tts>=0.1.0
httpx>=0.27.0
livekit-plugins-noise-cancellation